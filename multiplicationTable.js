multiplicationTable = function(size) {
  // insert code here
  let arr = [];
  let fullArr = [];
  for(i = 1; i <= size; i++){
    arr.push(i);
  };
  for(j = 1; j <= size; j++){
 function newFunct(num) {
   return num * j;}
 fullArr.push(arr.map(newFunct));
}return fullArr;
}
